# Plain Markdown Content

Dev blog content will start as plain `.md` files with frontmatter, not MDX. This keeps logs, projects, and pages easy to author and migrate while still allowing Astro to render static pages and fenced code blocks.
